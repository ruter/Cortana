version: '3.8'

services:
  cortana:
    build: .
    container_name: cortana-bot
    restart: unless-stopped
    env_file:
      - .env
    # Mount the source code for easier development (optional, remove for production)
    # volumes:
    #   - ./src:/app/src
